import { Trash2 } from 'lucide-react';
import { useApp } from '../app/AppContext';
export function SettingsPage() { const { state, setSettings, reset } = useApp(); return <div className="page narrow"><div className="page-header"><span className="eyebrow">Приложение</span><h1>Настройки</h1></div><div className="setup-card"><section><h2>Цветовая тема</h2><select value={state.settings.theme} onChange={(e) => setSettings({ theme: e.target.value as 'system'|'light'|'dark' })}><option value="system">Как в системе</option><option value="light">Светлая</option><option value="dark">Тёмная</option></select></section><section><h2>Локальные данные</h2><p className="hint">Удалит статистику, настройки и незавершённую сессию только на этом устройстве.</p><button className="button danger-button" onClick={() => { if (confirm('Удалить все локальные данные? Это действие нельзя отменить.')) reset(); }}><Trash2/> Удалить все данные</button></section></div></div>; }
